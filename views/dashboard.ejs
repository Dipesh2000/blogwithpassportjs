<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <header class="bg-blue-600 text-white py-4">
        <div class="container mx-auto flex justify-between items-center px-6">
            <div class="text-3xl font-bold">Blogin</div>
            <nav class="space-x-6">
                <a href="#" class="hover:text-gray-300">Home</a>
                <a href="/addUpdateBlog" class="hover:text-gray-300">Add Blogs</a>
            </nav>
            <a href="/logout" class="bg-white text-blue-600 py-2 px-4 rounded hover:bg-gray-200">Logout</a>
        </div>
    </header>

    <div class="flex">
        <aside class="w-64 bg-white shadow-lg py-8 px-4">
            <h2 class="text-xl font-semibold text-gray-800 mb-6">Menu</h2>
            <ul>
                <li><a href="#" class="block py-2 px-4 text-gray-700 hover:bg-gray-200 rounded">Dashboard</a></li>
                <li><a href="/addUpdateBlog" class="block py-2 px-4 text-gray-700 hover:bg-gray-200 rounded">Add Blog</a></li>
            </ul>
        </aside>
        <main class="flex-1 p-6">
            <section class="text-center mb-12">
                <h1 class="text-4xl font-bold text-gray-800 mb-4">Welcome to Blogin</h1>
                <p class="text-lg text-gray-600">Discover and share insightful articles on various topics.</p>
            </section>

            <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% blogs.map((blog) => { %>
                    <article class="bg-white shadow-md rounded-lg overflow-hidden">
                        <img src="<%= blog.image %>" class="w-full h-48 object-cover" alt="<%= blog.title %>">
                        <div class="p-6">
                            <h2 class="text-2xl font-semibold text-gray-800 mb-2"><%= blog.title %></h2>
                            <h3 class="text-xl text-gray-600 mb-4"><%= blog.author %></h3>
                            <p class="text-gray-700 text-base mb-4 line-clamp-3"><%= blog.content %></p>
                            <div class="flex justify-between mt-4">
                                <a href="/addUpdateBlog?id=<%= blog._id %>" class="bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600">Edit</a>
                                <a href="/deleteBlog?id=<%= blog._id %>" class="bg-red-500 text-white py-2 px-4 rounded hover:bg-red-600">Delete</a>
                            </div>
                        </div>
                    </article>
                <% }) %>
            </section>
        </main>
    </div>
</body>

</html>
